description: Digital inputs time stamp of the Blixt Breaker

# Used to map nodes to bindings
compatible: "blixt,breaker-input-timestamp"

properties:
    input-captures:
      type: phandle-array
      required: true
    input-capture-names:
      type: string-array
      required: true
    label:
      required: true
      type: string