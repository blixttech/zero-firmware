description: Digital inputs and output of the Blixt breaker

# Used to map nodes to bindings
compatible: "blixt,breaker-gpio"

properties:
    gpios:
      type: phandle-array
      required: true
    gpio-names:
      type: string-array
      required: true
    label:
      required: true
      type: string
