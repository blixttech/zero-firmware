description: Pin multiplexing configurations

# Used to map nodes to bindings
compatible: "blixt,pins"

properties:
    pinmuxs:
      type: phandle-array
      required: true
    pinmux-names:
      type: string-array
      required: true
    label:
      type: string
      required: true
